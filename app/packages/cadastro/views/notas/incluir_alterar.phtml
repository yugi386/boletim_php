<?php /*	
==========================================================================	
|	PÁGINA DE INCLUIR OU ALTERAR UMA FICHA DO CADASTRO
==========================================================================	
*/
	if ($view_tipo == '1') {						//	INCLUSAO
		echo "<H1>INCLUIR NOTA</H1>";
		$FW_operacao =  $view_redir . "incluir/"; 		//	VALOR PARA SETAR OPERACAO A SER FEITA...
	
	} else if ($view_tipo == '2') {					//	ALTERACAO
		echo "<H1>ALTERAR NOTA</H1>";		
		$FW_operacao =  $view_redir . "alterar/id/".$view_show_registro[0]['id']."/"; 		//	VALOR PARA SETAR OPERACAO A SER FEITA...
	}	
	
	$FW_checkbox = array();	//	indica campos checkbox...
	$FW_select = array();
	$FW_select = array(0 => 'aluno_id',
					   1 => 'disciplina_id'
					      );	// estes campos devem ser preenchidos na ordem em que aparecem no formulario preferencialmente (já há funcao para ordena-los)	
						  
	include("forms/formulario.php");				//	FORMULARIO DE ENTRADA DE DADOS
?>